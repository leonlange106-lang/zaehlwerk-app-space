import { VehicleSpecForm } from "../VehicleSpecForm";

export const metadata = {
  title: "Fahrzeug-Profil – MGflasher Log Analyzer",
};

export default function VehicleSpecPage() {
  return <VehicleSpecForm />;
}
